{/* <tbody>
          {list.length > 0 ? (
            list.map((user, index) => (
              <tr key={index}>

                <th scope="row">{index + 1}</th>
                <td>{user.username}</td>
                <td>{user.email}</td>
                <td>{user.password}</td>
                <td>{user.phone}</td>
                <td>{user.gender}</td>
                <td>{user.hobby?.toString()}</td>
                <td>{user.address}</td>
                <td>{user.city}</td>
                <td>
                  <button
                    className="btn btn-danger"
                    onClick={() => handleDelete(user.id)}
                  >
                    Delete
                  </button>
                  <button className="btn  ms-2 btn-warning" onClick={() => handleEdit(user.id)}>
                    Edit
                  </button>
                </td>
              </tr>
            ))
          ) : (
           
          )}
        </tbody> */}